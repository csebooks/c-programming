# Sample workflow for building and deploying a Hugo site to GitHub Pages
name: Publishing
on:
  # Runs on pushes targeting the default branch
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        hugo-version:
          - 'latest'
    steps:
      - uses: actions/checkout@v2

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: ${{ matrix.hugo-version }}
          extended: true

      - name: Download Theme
        run: |
          git clone https://github.com/techatpark/gurukulam
          find gurukulam/site/content.*/** -maxdepth 0 -type d -not -name courses -not -name "books"  -exec rm -rf {} \;
          rm -rf gurukulam/site/static/ && rm -rf gurukulam/site/layouts/index.html
          export BUILD_PRODUCTION=Y
          MATERIAL_PATH=$(git config --get remote.origin.url | cut -d. -f2 | cut -c 5-)

      - name: Build Books
        run: |
          for dir in content.*/; do mkdir -p temp/books/$MATERIAL_PATH && mv $dir/** temp/books/$MATERIAL_PATH && mv temp/** $dir/; done
          cp -rf content.* gurukulam/site/

      - name: Build Slides
        run: |
          for dir in slides/content.*/; do mkdir -p temp/courses/books/$MATERIAL_PATH && mv $dir/** temp/courses/books/$MATERIAL_PATH && mv temp/** $dir/; done
          cp -rf slides/content.* gurukulam/site/
          
      - name: Run Hugo
        working-directory: gurukulam/site
        run: env BUILD_PRODUCTION=Y hugo --themesDir ../..
 
      - name: Cleanup
        run: |
          rm -rf gurukulam/site/public/*.* && rm -rf gurukulam/site/public/tags && rm -rf gurukulam/site/public/en
          ls gurukulam/site/public

      - name: Deploy
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          source: "gurukulam/site/public"
          target: ${{ secrets.TARGET_DIR }}
          strip_components: 3

      - name: Upload Questions
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          source: "questions"
          target: ${{ secrets.QUESTIONS_DIR }}
